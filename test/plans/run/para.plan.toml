id = "run.para"
tier = "regression"
tags = ["run", "para"]

[[tests]]
name = "asian_option_mc"
kind = "behavior"
case = "../../cases/run/para/asian_option_mc.cml"
args = ["${case}"]
expect_exit = 0
xfail_reason = "This workload is intentionally heavy and exceeds the default local full-suite time budget on typical developer machines."
normalizers = ["strip_ansi", "normalize_newlines", "trim_trailing_space"]
timeout_ms = 30000

[[tests]]
name = "block_sort_merge"
kind = "behavior"
case = "../../cases/run/para/block_sort_merge.cml"
args = ["${case}"]
expect_exit = 0
normalizers = ["strip_ansi", "normalize_newlines", "trim_trailing_space"]
timeout_ms = 4000

[[tests]]
name = "bootstrap_medians"
kind = "behavior"
case = "../../cases/run/para/bootstrap_medians.cml"
args = ["${case}"]
expect_exit = 0
normalizers = ["strip_ansi", "normalize_newlines", "trim_trailing_space"]
timeout_ms = 4000

[[tests]]
name = "file_chunk_hash"
kind = "behavior"
case = "../../cases/run/para/file_chunk_hash.cml"
args = ["${case}"]
expect_exit = 0
normalizers = ["strip_ansi", "normalize_newlines", "trim_trailing_space"]
timeout_ms = 4000

[[tests]]
name = "merge_sort"
kind = "behavior"
case = "../../cases/run/para/merge_sort.cml"
args = ["${case}"]
expect_exit = 0
normalizers = ["strip_ansi", "normalize_newlines", "trim_trailing_space"]
timeout_ms = 20000

[[tests]]
name = "monte_carlo_pi"
kind = "behavior"
case = "../../cases/run/para/monte_carlo_pi.cml"
args = ["${case}"]
expect_exit = 0
normalizers = ["strip_ansi", "normalize_newlines", "trim_trailing_space"]
timeout_ms = 25000
