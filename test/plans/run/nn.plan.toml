id = "run.nn"
tier = "regression"
tags = ["run", "nn"]

[[tests]]
name = "lr"
kind = "behavior"
case = "../../cases/run/nn/lr.cml"
args = ["${case}"]
expect_exit = 0
normalizers = ["strip_ansi", "normalize_newlines", "trim_trailing_space"]
timeout_ms = 4000

[[tests]]
name = "lrit"
kind = "behavior"
case = "../../cases/run/nn/lrit.cml"
args = ["${case}"]
expect_exit = 0
normalizers = ["strip_ansi", "normalize_newlines", "trim_trailing_space"]
timeout_ms = 4000

[[tests]]
name = "mnist"
kind = "behavior"
case = "../../cases/run/nn/mnist.cml"
args = ["${case}"]
expect_exit = 0
normalizers = ["strip_ansi", "normalize_newlines", "trim_trailing_space"]
timeout_ms = 4000

[[tests]]
name = "tensor_smoke"
kind = "behavior"
case = "../../cases/run/nn/tensor_smoke.cml"
args = ["${case}"]
expect_exit = 0
normalizers = ["strip_ansi", "normalize_newlines", "trim_trailing_space"]
timeout_ms = 4000

[[tests]]
name = "mnist_py"
kind = "behavior"
case = "../../cases/run/nn/mnist_py.cml"
args = ["${case}"]
expect_exit = 0
xfail_reason = "Known Python-integration regression in the MNIST helper path."
normalizers = ["strip_ansi", "normalize_newlines", "trim_trailing_space"]
timeout_ms = 4000

[[tests]]
name = "svm"
kind = "behavior"
case = "../../cases/run/nn/svm.cml"
args = ["${case}"]
expect_exit = 0
xfail_reason = "Known neural-network runtime regression in the SVM helper path."
normalizers = ["strip_ansi", "normalize_newlines", "trim_trailing_space"]
timeout_ms = 4000
