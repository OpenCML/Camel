# Test Layout

- `cases/` contains source assets.
- `plans/` contains suite-level test plans.
- `tools/` contains the runner and helpers.
- `results/` stores execution logs.
- `vars.toml` defines reusable command fragments.
